# Frontend

Vue 3 + TypeScript + Vite 前端工程骨架。

D6 当前只提供 GIS 工作台壳子，不接入真实地图、底图或后端数据。

## 启动

```text
npm install
npm run dev
```

默认访问：

```text
http://localhost:5173
```

## 配置

天地图 token 通过环境变量提供：

```text
VITE_TIANDITU_TOKEN=
```

真实 token 不得提交到 GitHub。
