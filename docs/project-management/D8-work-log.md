# D8 工作记录：前后端联通

日期：2026-07-01
分支：`docs/d8-frontend-backend-integration`

## 当日目标

在 D7 本地运行验证基础上，完成第一条真实前后端联通链路：前端通过统一 API 客户端请求后端 `/api/v1/meta`，并在工作台页面展示后端返回的项目元信息。

## 完成内容

1. 创建 D8 工作分支；
2. 新增后端本地 CORS 配置；
3. 允许 `http://127.0.0.1:5173` 和 `http://localhost:5173` 访问后端；
4. 新增前端 `httpClient`；
5. 新增前端 `metaApi`；
6. 新增前端 `MetaResponse` 类型；
7. 修改工作台页面，从后端读取项目名称、研究区、数据版本和阶段；
8. 增加前端后端连接状态展示；
9. 修复前端页面早期中文编码显示问题；
10. 运行前端类型检查；
11. 运行前端生产构建；
12. 验证后端 CORS 响应头；
13. 验证前端页面 HTTP 200；
14. 记录 D8 审计报告。

## 验证结果

| 检查项 | 结果 | 说明 |
|---|---|---|
| 后端 CORS 配置 | 通过 | `/api/v1/meta` 对本地前端 Origin 返回允许访问响应头 |
| 前端类型检查 | 通过 | `npm.cmd run typecheck` 成功 |
| 前端构建 | 通过 | `npm.cmd run build` 成功 |
| 前端页面访问 | 通过 | `http://127.0.0.1:5173/` 返回 HTTP 200 |
| 后端 meta 接口 | 通过 | `http://127.0.0.1:8000/api/v1/meta` 可访问 |

## 当前访问地址

```text
前端预览：http://127.0.0.1:5173/
后端健康：http://127.0.0.1:8000/health
后端元信息：http://127.0.0.1:8000/api/v1/meta
```

## 范围边界

D8 只完成前后端元信息联通，不接入真实 GIS 数据、不接入 MapLibre、不加载天地图、不连接 PostGIS、不实现道路或网格查询。

## 发现的问题与处理

| 问题 | 等级 | 处理 |
|---|---|---|
| 旧后端进程占用 8000 | Warning | 查找并停止旧进程后，启动包含 CORS 的 D8 后端 |
| 临时使用 8001 验证 CORS | Info | 验证完成后已停止临时 8001 服务 |
| 前端页面中文编码异常 | Warning | D8 重写 `App.vue`，恢复正常中文内容 |

## 下一步

D9 建议接入基础地图模块：安装 MapLibre GL JS，创建地图容器，先加载空白底图或 OSM 公开底图，不接入真实业务数据。
